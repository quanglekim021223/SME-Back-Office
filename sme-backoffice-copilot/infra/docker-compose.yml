name: sme-backoffice-copilot

services:
  frontend:
    build:
      context: ../frontend
    user: "0:0"
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:8000
      WATCHPACK_POLLING: "true"
    ports:
      - "3000:3000"
    command: ["npm", "run", "dev"]
    depends_on:
      backend:
        condition: service_healthy
    volumes:
      - ../frontend:/app
      - frontend_node_modules:/app/node_modules
      - frontend_next:/app/.next

  backend:
    build:
      context: ../backend
    env_file:
      - path: ../.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://sme:sme@postgres:5432/sme_backoffice
      UPLOAD_STORAGE_ROOT: /data/uploads
      WORKFLOW_QUEUE_MODE: celery
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      OUTBOX_DISPATCHER_ENABLED: "false"
      PROVIDER_RATE_LIMIT_REDIS_URL: redis://redis:6379/2
      PROVIDER_RATE_LIMIT_ENABLED: "true"
    ports:
      - "8000:8000"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    volumes:
      - ../backend/app:/app/app
      - uploads_data:/data/uploads
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')",
        ]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s

  worker:
    # PaddlePaddle's published Linux ARM64 wheel segfaults during OCR inference.
    # Keep the local benchmark worker on x86_64 until upstream ships a fixed wheel.
    platform: linux/amd64
    build:
      context: ../backend
    command:
      [
        "celery",
        "-A",
        "app.workers.celery_app:celery_app",
        "worker",
        "--loglevel=INFO",
        "--pool=${CELERY_WORKER_POOL:-solo}",
        "--concurrency=${CELERY_WORKER_CONCURRENCY:-1}",
        "--queues=document-processing-high,document-processing-medium,document-processing-low",
      ]
    env_file:
      - path: ../.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://sme:sme@postgres:5432/sme_backoffice
      UPLOAD_STORAGE_ROOT: /data/uploads
      WORKFLOW_QUEUE_MODE: celery
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      OUTBOX_DISPATCHER_ENABLED: "false"
      PROVIDER_RATE_LIMIT_REDIS_URL: redis://redis:6379/2
      PROVIDER_RATE_LIMIT_ENABLED: "true"
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      PADDLEOCR_LANGUAGE: pt
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    volumes:
      - ../backend/app:/app/app
      - uploads_data:/data/uploads
      - paddleocr_cache:/home/app/.paddlex

  outbox:
    build:
      context: ../backend
    command: ["python", "-m", "app.workers.outbox_dispatcher"]
    env_file:
      - path: ../.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://sme:sme@postgres:5432/sme_backoffice
      WORKFLOW_QUEUE_MODE: celery
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      OUTBOX_DISPATCHER_ENABLED: "true"
      PROVIDER_RATE_LIMIT_REDIS_URL: redis://redis:6379/2
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    volumes:
      - ../backend/app:/app/app

  migrate:
    build:
      context: ../backend
    command: ["alembic", "upgrade", "head"]
    env_file:
      - path: ../.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://sme:sme@postgres:5432/sme_backoffice
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  redis:
    image: redis:7-alpine
    ports:
      - "${REDIS_HOST_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: sme_backoffice
      POSTGRES_USER: sme
      POSTGRES_PASSWORD: sme
    ports:
      - "${POSTGRES_HOST_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sme -d sme_backoffice"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  frontend_node_modules:
  frontend_next:
  postgres_data:
  uploads_data:
  paddleocr_cache:
